---
framework: mocha
preRun:
     command:
        - npm install
preMerge:
  pattern:
    - "./test/index.js"
postMerge:
  pattern: 
    - "./test/index.js"
postRun:
     command:
        - node --version
nodeVersion: 12.22.7
version: 1.0
